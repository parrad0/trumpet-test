# Automatically install peer dependencies
auto-install-peers=true

# Use legacy peer deps resolution (less strict)
legacy-peer-deps=true

# Don't show warnings and notifications
fund=false
audit=false
deprecated=false

# Reduce npm log level to minimize warnings
loglevel=error